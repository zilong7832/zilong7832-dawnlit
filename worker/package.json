{
  "name": "dawnlit-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute dawnlit --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute dawnlit --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
